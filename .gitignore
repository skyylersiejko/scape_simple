# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# dotenv environment variables file
.env

# Electron build output — installers are ~100 MB each and are published as
# GitHub Release assets, not committed to the repo.
bot-demo/release/
